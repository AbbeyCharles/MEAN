<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Document</title>
</head>
<body>
  <div class="wrapper">
    <% for (dog in dogs) { %>
      <h1>Edit <%= dogs[dog].name %>'s Profile</h1>
      <form action="/<%= dogs[dog].id %>" method="post">
        Name: <input type="text" name="name" value="<%= dogs[dog].name %>">
        Age: <input type="number" name="age" value="<%= dogs[dog].age %>">
        Comments: <input type="textarea" name="comment" value="<%= dogs[dog].comment %>">
        <input type="submit" value="Edit This Dog">
      </form>
    <% } %>
    <button><a href="/">Home</a></button>
  </div>
</body>
</html>
